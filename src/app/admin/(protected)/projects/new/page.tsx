import { AdminRoutePlaceholder } from "@/components/admin/admin-route-placeholder";

export default function AdminProjectNewPage() {
  return (
    <AdminRoutePlaceholder
      title="New project"
      description="This route will host the full project creation workflow, including screenshots, metadata, tech stack, publish state, and featured visibility."
    />
  );
}
