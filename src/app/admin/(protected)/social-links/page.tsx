import { AdminRoutePlaceholder } from "@/components/admin/admin-route-placeholder";

export default function AdminSocialLinksPage() {
  return (
    <AdminRoutePlaceholder
      title="Social links"
      description="Manage recruiter-facing social destinations, labels, ordering, and visibility from this CMS route."
    />
  );
}
